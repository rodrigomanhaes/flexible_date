br:
  flexible_date:
    configuration:
      format: "%d/%m/%Y"
      datetime_format: "%d/%m/%Y %H:%M:%S"
    messages:
      with_suffix:
        invalid: "inválida."
      without_suffix:
        invalid: "inválida."

